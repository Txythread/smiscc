extern "C" "_start" func start() {
//	/*let c = */add(5, 6);
	let c = 7;
	was_geht(5);
}

func add(k: u32, b: u32) {
	let x = k;
	let y = b;
	k + b;
	x - y;
}

func was_geht(a: u32) {
	if a {
		exit a;
	}
	exit 1;
}
